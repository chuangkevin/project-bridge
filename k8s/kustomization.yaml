apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: project-bridge

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - secret.yaml

images:
  - name: srvhpgit1:32050/project-bridge
    newTag: latest

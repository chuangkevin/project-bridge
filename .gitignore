node_modules
dist
*.db
.env
data/
test-results/
.claude/settings.local.json
.ai-review/

# Test artifacts
*.traineddata
.playwright-mcp/
packages/server/test-output.html
packages/server/test-e2e-generate.js
*.png
!packages/client/public/*.png

# Crawl artifacts
/d:/tmp/houseprice-*

# Superpowers brainstorm artifacts (per-session mockups + server state)
.superpowers/

# TypeScript build cache
**/tsconfig.tsbuildinfo

# Runtime SQLite DB (new server)
# Un-ignore the new server's data/ dir (the bare "data/" above is too broad),
# then re-ignore only the runtime DB files inside it.
!packages/server/data/
packages/server/data/*.db
packages/server/data/*.db-wal
packages/server/data/*.db-shm

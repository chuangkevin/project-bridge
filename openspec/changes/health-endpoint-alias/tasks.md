## 1. Health endpoint

- [x] 1.1 Add `/health` alias that returns the same backend JSON as `/api/health`.
- [x] 1.2 Ensure SPA fallback no longer captures `/health`.

## 2. Deployment and docs

- [x] 2.1 Update deploy health check to use `/health`.
- [x] 2.2 Sync version/docs/memory.
- [ ] 2.3 Build, commit, push, and verify live.
